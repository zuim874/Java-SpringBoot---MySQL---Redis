2026.8.5

========== 问题汇总：
并发问题解决过程中产生的多个问题记录

========== 解决记录：
1. ActiveMQConfig 消息转换器需要 import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;
   处理 LocalDateTime 时间序列化问题

2. 手机端和电脑端打开注册页，输入同一邮箱并点发送验证码，后台真的发送了两条邮件：
   原因：邮箱本身是不区分大小写的，但是在写入 redis 的过程中，redis 会区分大小写，
         产生了以为不是同一邮箱的误判
   解决方法：
        /**
         * @author ZuiM
         * @param email
         * @return String：非gmail邮箱转小写
         */
        public String dealEmail(String email) {
            if (!email.contains("@gmail.com")) {
                return email.toLowerCase(Locale.ROOT);
            }
            else {
                return email;
            }
        }

3. （原记录第 3 条为空，保留原始状态）
