-- =============================================
-- 为 sys_user 增加余额字段（模拟货币）
-- 说明：已建库环境执行本脚本即可，无需重建表
-- =============================================
ALTER TABLE sys_user ADD COLUMN balance DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '账户余额（元，充值/支付用）';